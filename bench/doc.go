// Package bench contains performance benchmarks for the do dependency
// injection library and its stacktrace helper. It lives in its own package so
// the benchmarks exercise the library exclusively through its public API.
package bench
